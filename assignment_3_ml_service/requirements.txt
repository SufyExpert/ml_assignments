# --- Web service ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4

# --- ML / data ---
# IMPORTANT: these are pinned to match the exact versions used to train and
# pickle model/model.joblib (see model/metadata.json -> package_versions).
# joblib-persisted scikit-learn pipelines are not always compatible across
# different major/minor scikit-learn versions, so the serving environment
# must match the training environment.
scikit-learn==1.8.0
pandas==3.0.2
numpy==2.4.4
joblib==1.5.3
scipy==1.17.1
imbalanced-learn==0.14.2

# --- Testing ---
pytest==8.3.4
httpx==0.28.1
